name: Deploy Discord Bot

on:
  push:
    branches:
      - main

jobs:
  build:
    if: false  # Отключаем все действия в этом job
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '20'

    - name: Install dependencies
      run: npm install

    - name: Start Bot
      run: node app.js  # Запускаем бота
      env:
        DISCORD_TOKEN: ${{ secrets.DISCORD_TOKEN }}
        MONGODB_CONNECT: ${{ secrets.MONGODB_CONNECT }}
        CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}

  deploy:
    if: false  # Отключаем этот job
    runs-on: ubuntu-latest
    steps:
      - name: No-op Step
        run: echo "This job will not run."

  test:
    if: false  # Отключаем этот job
    runs-on: ubuntu-latest
    steps:
      - name: No-op Step
        run: echo "This job will not run."
