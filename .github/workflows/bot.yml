name: Discord Bot

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'  # Или выше, если вы используете последние функции

      - name: Install dependencies
        run: npm install

      - name: Run Discord bot
        env:
          TOKEN: ${{ secrets.TOKEN }}
        run: node index.js